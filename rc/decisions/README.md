# RC decisions

RC-only decisions. Naming: `YYYY-MM-DD-topic.md` (same frontmatter pattern as [decisions/](../../decisions/) at repo root).

Cross-repo decisions (e.g. RC timeline DTO) may remain at repo root with an RC tag in frontmatter.
